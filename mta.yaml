_schema-version: "3.2"
ID: pricingworkflow
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: pricingworkflow-destination-content
  type: com.sap.application.content
  requires:
  - name: pricingworkflow-destination-service
    parameters:
      content-target: true
  - name: pricingworkflow-repo-host
    parameters:
      service-key:
        name: pricingworkflow-repo-host-key
  - name: pricingworkflow-uaa
    parameters:
      service-key:
        name: pricingworkflow-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: pricingworkflow_html_repo_host
          ServiceInstanceName: pricingworkflow-html5-srv
          ServiceKeyName: pricingworkflow-repo-host-key
          sap.cloud.service: pricingworkflow
        - Authentication: OAuth2UserTokenExchange
          Name: pricingworkflow_uaa
          ServiceInstanceName: pricingworkflow-xsuaa-srv
          ServiceKeyName: pricingworkflow-uaa-key
          sap.cloud.service: pricingworkflow
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: pricingworkflow-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: pricingworkflow-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - pricingworkflow.zip
      name: pricingworkflow
      target-path: resources/
- name: pricingworkflow
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: pricingworkflow-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: pricingworkflow-destination-service
    service-plan: lite
- name: pricingworkflow-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: pricingworkflow-xsuaa-srv
    service-plan: application
- name: pricingworkflow-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: pricingworkflow-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
